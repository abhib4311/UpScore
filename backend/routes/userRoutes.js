// planRoutes.js
const express = require("express");
const router = express.Router();

// Define your routes for plan management
// Example: POST /api/plans/upgrade for upgrading the user's plan

module.exports = router;
